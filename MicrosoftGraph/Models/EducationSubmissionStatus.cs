namespace GitHubTodoDemo.MicrosoftGraph.Models {
    public enum EducationSubmissionStatus {
        Working,
        Submitted,
        Released,
        Returned,
        UnknownFutureValue,
        Reassigned,
    }
}
