namespace GitHubTodoDemo.MicrosoftGraph.Models.ExternalConnectors {
    public enum ConnectionState {
        Draft,
        Ready,
        Obsolete,
        LimitExceeded,
        UnknownFutureValue,
    }
}
