name: Gradle Build And Test

on:
  push:
    branches: [main]
    paths: 
      - 'java/**'
      - 'java/*'
      - '.github/**'
  pull_request:
    branches: [main]
    paths:
      - 'java/**'
      - 'java/*'
      - '.github/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
    - uses: baywet/setup-kiota@v0.1.0
    - run: kiota update
      working-directory: java
    - uses: actions/checkout@v4
    - name: Setup JDK
      uses: actions/setup-java@v3
      with: 
        java-version: 18
        distribution: 'adopt'
        cache: gradle
    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew
      working-directory: java
    - name: Build with Gradle
      run: ./gradlew --no-daemon build
      working-directory: java
